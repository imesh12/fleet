# Layout And Navigation

Updated:

- Protected app layout uses semantic background and responsive main padding.
- Sidebar uses the primary token and stronger active/coming-soon states.
- Topbar uses a sticky translucent surface with tokenized borders.
- `ModulePageHeader` provides the new header foundation with title, eyebrow, breadcrumbs, status, actions, and organization context.

Existing navigation data, RBAC filtering, coming-soon routing, and organization behavior remain unchanged.

Known limitation:

The sidebar remains primarily desktop-oriented. A dedicated mobile drawer is recommended for a follow-up polish pass.
