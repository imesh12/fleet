# Dashboard Placeholder

Route:

```text
/dashboard
```

Backend call:

```http
GET /api/v1/admin/dashboard/summary
```

The dashboard renders summary cards and a raw backend response preview to confirm connectivity before full page rebuilds begin.
