# Stage 24 Verification

Commands:

```powershell
npm.cmd install
npm.cmd run typecheck
npm.cmd run build
```

For local frontend development:

```powershell
npm.cmd run dev
npm.cmd run dev:web
```

The API runs on port `3000` and the frontend runs on port `3001`.
